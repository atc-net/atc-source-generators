namespace Atc.SourceGenerators.EnumMapping.Enums;

/// <summary>
/// Pet status in the API layer (DTO).
/// </summary>
public enum PetStatusDto
{
    Unknown,
    Available,
    Pending,
    Adopted,
}